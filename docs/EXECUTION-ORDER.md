# THE EXECUTION ORDER — knock it out, top to bottom

**What this is (Robert, 2026-07-21):** "I wanna plan this stuff out so we can just knock it out in a logical order **without any input from me**." This is that order. Every item below is **fully decided and fully specced** — the loop (or any session) works this file top to bottom, no questions asked. Anything that still needs Robert is in **PARKED**, not in the queue.

**The ritual per block (unchanged law):** build → gates (`npx tsc --noEmit && npx vitest run && npm run lint && npm run build`) → live/deterministic proof → flip the rows in the spec + STATUS.md → one commit per slice, stage by name → push. Marks are sidegrades. New sim behavior gates so existing matches stay byte-identical where the spec says so. **No purple, ever.** All new UI composes from `UX-LANGUAGE.md` primitives — no bespoke elements (Law 7).

## PROGRESS (2026-07-21 — the coding run so far, all live-verified + pushed)

- **✅ B1 THE HUD BUILD — DONE.** Weapon-cam plate (real 3D model, maker-tinted chrome) + THE ONE METER (segmented bar, amber lead-notch) carrying reload / Impact Charge / vehicle-WPN / LSW-signature; 2 of the 7 style violations killed. `b67cc18`, `cfa0a8b`.
- **✅ B2 THE AIR FIX — DONE.** Sanctuary Law (`p.airScaled` gate — the no-band-gate bug fixed), BAND_ALT raised, cloud shelf y11-13, shadow altimeter, ALT chip. `472d864`, `a0cbe12`, `89e717c`. (Bomb fall-time + ring folds into B8.)
- **🔨 B3 THE DEATH SHOW — started.** The DeathReport captures (weaponId/killerId/dist/timeAlive/killerVehicle now ride the death event; `Soldier.spawnedAt`), killfeed range + bleedout-weapon fix. `7002842`. Director branches / KILL cam / collapses still queued.
- **🔨 B4 WEAPON MEMORY — slice 1 done.** The gun's plate engraves "N CONFIRMED · <longest>u" (family-gated) + hull-type stamps, off the DeathReport. `122dcc0`. Sim serial registry + drops = later slices.
- **✅ SIDE FIXES.** THE ROSTER LAW (iron never with zombies unless opted in) `b234230`; DEATH RE-SELECT (pick class between prints) `c38766a`; THE AAR v1+v2 (per-weapon kills, longest shot, hulls, moments, **nemesis + prey duels**) `b04a195`+`f3f3172`.
- **📄 Planning:** `PATTERN-REGISTRATION.md` (FORM P-1 personas → B10.5).

| # | Block | Spec (the law) | Why this position |
|---|---|---|---|
| **B1** | **THE HUD BUILD** — the decided kit: the info-rich **segmented METER** component (amber lead-notch accent, number, state word, release hint) replacing EVERY fill readout (reload bar, LSW bar, grenade pip-sweep, Impact Charge text, vehicle WPN bar); the **weapon-cam plate** (live 3D model of the equipped gun, bottom-right) wearing the **manufacturer frame** (`parseBrand`, 6 empty shells in `docs/reference/hud/`); fix the **7 standing style violations** UX-LANGUAGE found. | UX-LANGUAGE §6/§8 + reference/hud/README | Fully decided by Robert (meter A, weapon-cam, per-maker frames). Everything downstream (nameplates, cards, death UI) lives in or on this. |
| **B2** | **THE AIR FIX** — raise the bands (BAND_ALT ladder), the **Sanctuary Law** (projectile `reach`: small arms cap at band 1; only SAMs/MANPADS/band-3 aircraft touch band 3 — kills the no-band-gate bug at the vehicle hit test), the **cloud shelf** between bands 2-3, band-scaled shadows, bomb fall-time + tightening ground ring, the ALT chip (backlog 1.7c). | VERTICAL-WAR slice 1 | Robert's named "root problem with all of flight" (the bomber is helpless because altitude is cosmetic today). Small, self-contained, huge feel. |
| **B3** | **THE DEATH SHOW** — the canonical `DeathReport` object + missing captures; the killcam **director branch table** (Ride the Round / The Autopsy / The Wide / spawn-kill quick-cut); the killer-facing **KILL cam**; per-weapon **collapse variants**; **corpses linger 20-30s** (⚠ this also unblocks the outbreak's corpse-state visuals); killer's **service card**; death constellation + autopsy line. | DEATH-DATA.md (+ MASTER-BACKLOG W2 items 2.1-2.5, 9.6/9.7/9.15) | Robert's active ask ("where are we with the death stuff"). Data layer (DeathReport) feeds B4's gun ledger too. |
| **B4** | **WEAPON MEMORY** — slice 1 gun serials + kill ledger + nameplate on the weapon-cam plate; slice 2 drops beside the corpse + E-hold swap; slice 3 notch decals + inspect ledger + the Dossier armory shelf. Perf verdict FITS NOW; ship-gate: bench run with 16 seeded drops, <0.3ms regression. | WEAPON-MEMORY.md | Needs B1 (the nameplate's home) and B3's DeathReport (clean kill feed into the ledger). The revenge loop lands here. |
| **B5** | **WEAPON CARDS** — slice 1 the card component + armory usage; slice 2 the portrait pipeline + codex + killfeed hover. (The service-history layer and killcam-footer card ride B4's ledger.) | WEAPON-CARDS.md | Pure presentation on top of B1's renders + B4's ledgers. |
| **B6** | **SIGHT** — the biggest foundation still open: the sight split (3D = you, minimap = team), contacts HOLD then FADE (never blink), the darkness/vision cone, the second-storey fishbowl fix. | STATUS.md C1 / backlog W0.2, #46 | Unblocks the flashlight (outbreak §10), squads' shared intel (B7 slice 3), and sonar ghost-marks (B9). Scheduled after the momentum blocks because it's the riskiest rewire. |
| **B7** | **SQUADS** — slice 1 assignment + spawn wave + the squad strip; slice 2 the four orders (MOVE/HOLD/HELP/FOCUS) as sim state (`world.issueOrder`) + bot obedience; slice 3 shared intel (needs B6) + chatter + identity. | SQUADS.md | Slices 1-2 stand alone; slice 3 consumes B6. Personas hook into B10's voices later. |
| **B8** | **BIG MOMENTS** — slice 1 the one screen-shake grammar + generalized delayed-arrival audio; slice 2 THE NUKE (arming ritual → fall → whiteout → **mushroom cloud** → late thud → the scar); then the lesser moments (pod, orbital, SECTOR LOST sky). | BIG-MOMENTS.md | Needs B2 (the bomber can actually reach altitude before its bomb deserves a mushroom cloud). |
| **B9** | **BOATS → SUBMARINE** — slice 2 the boat lineup (Marlin sub-hunter with sonar + depth charges FIRST, Mudskipper barge); slice 3 the Moray sub (3 depths on the Q/E grammar, sonar as sight, wake-visible torpedoes, depth-charge counterplay). Carriers stay PARKED. | VERTICAL-WAR slices 2-3 | The counter ships before the threat (Marlin before Moray). Sonar marks use B6's ghost grammar. |
| **B10** | **VO WAVE 1** — the announcer + per-class bark set (P0-P2 tiers only) batch-generated via `expressive-tts`, the 4-lane voice bus + cooldown doctrine wired, manifest committed so voices are re-castable. | VO-CATALOG.md | After squads exist so squad comms slots ship in the same batch. Needs Replicate available — if not, skip and return. |
| **B10.5** | **THE PATTERN** — FORM P-1 Enlistment Office (slice P1: Pattern Card + the diegetic intake screen), the authored bot starter cast (P2), then the author+voice passes onto the VO manifest (P3-P4, the per-player generated-VO killer feature). | PATTERN-REGISTRATION.md | P1-P2 need nothing and can ship any time; P3-P4 ride B10's pipeline. Cast picks (Odette + the mean one + 6) are Robert-approvable but defaults are authored. |
| **B11** | **THE WAR** — kill the time-skip (easiest first), the 3×3 war board, clones-are-the-currency (reserves, FLESH vs CHROME), pass escalation through the gods, science missions v1, class-change-by-request, the two faction leaders (voiced via B10's pipeline), rank on the shoulder, bots-read-as-robots, Iron Eaters finished. | MASTER-BACKLOG Wave 3 (WAR.md) | The campaign layer that makes matches mean something — placed after the moment-to-moment game is whole. |
| **B12** | **PERF & NET PREREQUISITES** — the remaining unconverted scans (explode, vehicles/turrets, recon auras, updateLastSeen), snapshot-size work, and the netcode prerequisite refactors (backlog 8.2) — landed while cheap. | OPTIMIZATION-AUDIT.md / issues | Keeps the door open for real multiplayer without blocking feature momentum. |

**Standing verification tools:** zombie bench + tracker for frame-time claims · `window.__ww` + Playwright for live visual proof (preview pane freezes RAF — see memory) · deterministic pump for harness animation checks · carve arenas for RNG-coupled tests · inert witness in 1-human-team tests.

## PARKED (needs Robert, or blocked — NOT in the queue)

- **Faction & manufacturer names** — come from Robert's *Consequences of Failure* governments spreadsheet. Everything ships with placeholder strings until he renames.
- **PvP Control Struggle minigame** (outbreak §15.1-4) — fairness hinges on netcode; the Bite Struggle half is shipped.
- **True neutral third faction** — a combat-core refactor (`1 - team` everywhere); high regression risk against 1,355 tests. Deliberate decision required.
- **Aircraft carriers** — deferred by design (VERTICAL-WAR slice 4: deck pad, no interior, later).
- **Mixed magazines / per-type ammo reserves** (outbreak §11.3).
- **Monetization** (charge-to-wipe-your-character) — retention hook noted in SQUADS/VO docs; needs Robert.
- **The JA2 dialogue document** — if Robert finds it, it merges into VO-CATALOG's trigger tables.
- **Dedicated servers / analytics dashboards** — infrastructure, out of scope for the browser build (sim is already deterministic-ready).

*Maintenance rule: when a block finishes, check its box here, flip its spec's rows, and the next block starts — no check-in required. New ideas enter as new rows only if fully specced; otherwise they go to PARKED or the backlog.*
