/** Dev-only Vite plugin: the Sound Editor's regenerate-by-prompt API.
 *  Typed loosely (the repo has two Vite copies — vite + vitest's — whose Plugin
 *  types don't unify; `any` satisfies PluginOption from either). */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export function soundEditorPlugin(): any;
