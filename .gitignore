node_modules/
dist/
*.log
.DS_Store

# playwright-mcp screenshot scratch
.playwright-mcp/

# ElevenLabs A/B/C sound candidates — working artifacts for the review page;
# the chosen ones get baked into public/audio/*.wav via tools/apply-picks.mjs
public/audio/variants/

# agent worktrees + local claude state
.claude/

# §11.5 the server's theatre file — the War Room's campaign state, per machine
.warroom-campaign.json
