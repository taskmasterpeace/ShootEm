node_modules/
dist/
*.log
.DS_Store

# playwright-mcp screenshot scratch
.playwright-mcp/

# ElevenLabs A/B/C sound candidates — working artifacts for the review page;
# the chosen ones get baked into public/audio/*.wav via tools/apply-picks.mjs
public/audio/variants/

# agent worktrees + local claude state
.claude/

# §11.5 the server's theatre file — the War Room's campaign state, per machine
.warroom-campaign.json
tools/atlas/

# Ascendant live-verify screenshots (Playwright debug artifacts)
/*-hero.jpeg
/*-hero2.jpeg
/*-ascended.jpeg

# recovered maps WIP (kept out of git; see README)
/.maps-wip-recovered/

# converted asset intermediates (source zips + extracted blends + glb staging).
# Source material stays OUT of git; only the converted .glb under public/ ships.
assets/raw/
assets/glb/
assets/*.zip
